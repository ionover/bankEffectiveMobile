info:
  name: cteate user
  type: http
  seq: 2

http:
  method: POST
  url: http://10.10.10.61:8084/users
  body:
    type: json
    data: |-
      {
        "name":"name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
