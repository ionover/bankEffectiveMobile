info:
  name: transferMoney
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8084/operations/transferMoney
  body:
    type: json
    data: |-
      {
        "cardFromId": 1,
        "cardWhereId": 2,
        "money": 500
      }
  auth:
    type: bearer
    token: ey

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
