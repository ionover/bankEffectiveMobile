info:
  name: patch user
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8084/users/2
  body:
    type: json
    data: |-
      {
        "name": "name12"
      }
  auth:
    type: bearer
    token: ey

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
