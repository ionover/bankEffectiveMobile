info:
  name: get users
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8084/users
  auth:
    type: bearer
    token: ey

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
