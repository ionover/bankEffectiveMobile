info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8084/oauth/login
  body:
    type: json
    data: |-
      {
        "login": "systemAdmin@mail.com",
        "password": "12"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
