info:
  name: get card
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8084/cards
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ey

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
