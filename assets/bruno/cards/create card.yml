info:
  name: create card
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8084/cards
  body:
    type: json
    data: |-
      {
        "number":"1243",
        "owner":1
      }
  auth:
    type: bearer
    token: ey

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
