info:
  name: patch card
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8084/cards/1/status
  body:
    type: json
    data: '"ACTIVE"'
  auth:
    type: bearer
    token: ey

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
